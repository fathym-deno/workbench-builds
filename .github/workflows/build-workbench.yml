name: build-workbench
on:
  workflow_dispatch:
    inputs:
      source_repo:
        description: 'owner/repo or full clone URL of the workbench repo'
        required: true
      ref:
        description: 'commit sha (already resolved by the SOP)'
        required: true
      image_tag:
        description: 'wb-{APISlug}:{sourceHash}'
        required: true
      dockerfile:
        description: 'Resolved Dockerfile contents (per-mode -> workbench -> OI default)'
        required: true

permissions:
  contents: read
  packages: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout workbench repo
        # Use WORKBENCH_CHECKOUT_TOKEN when set (fine-grained PAT scoped to
        # the private workbench source repos, e.g. fathym-deno/fathym-dev-space).
        # Falls back to the workflow's default GITHUB_TOKEN for public repos.
        uses: actions/checkout@v4
        with:
          repository: ${{ inputs.source_repo }}
          ref: ${{ inputs.ref }}
          token: ${{ secrets.WORKBENCH_CHECKOUT_TOKEN || secrets.GITHUB_TOKEN }}

      - name: Write resolved Dockerfile
        # Route the Dockerfile through an env var so its raw content
        # (including any `"`, `$`, backticks) survives yaml + shell parsing
        # verbatim. Direct `${{ inputs.dockerfile }}` in the run script
        # gets substituted into the shell text and any inner `"` breaks
        # out of the surrounding shell string.
        env:
          DOCKERFILE_CONTENT: ${{ inputs.dockerfile }}
        run: |
          printf '%s' "$DOCKERFILE_CONTENT" > ./Dockerfile.oi

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build + push image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile.oi
          push: true
          tags: ghcr.io/fathym-deno/${{ inputs.image_tag }}
